export function PlaceholderPage({ title }) {
  return (
    <main className="placeholder-page">
      <section className="placeholder-card">
        <p>RainGuard Admin</p>
        <h2>{title}</h2>
        <span>This section is ready for the next dashboard screen.</span>
      </section>
    </main>
  )
}
